invoicing-system:
  database: jpa
spring:
  datasource:
    url: jdbc:postgresql://localhost:${DB_PORT:5432}/test
    username: test
    password: test
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQL95Dialect
